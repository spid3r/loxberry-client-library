# Skip lifecycle scripts from dependencies (postinstall, preinstall, etc.).
# Safer supply chain; this project does not rely on install-time native builds.
# Your own `npm run build` / `npm test` scripts are unaffected.
ignore-scripts=true
